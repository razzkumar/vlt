# Example vault-env configuration file
vault:
  addr: "http://localhost:8200"
  # Uncomment and set these for GitHub auth:
  # github_token: "ghp_xxxxxxxxxxxxx"
  # auth_method: "github"

kv:
  mount: home

secrets:
  - path: myapp/db_password
